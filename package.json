{
  "name": "svg-sheet",
  "version": "0.1.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "svgo": "^3.0.2",
    "svgson": "^5.2.1"
  },
  "scripts": {
    "min": "svgo -f ./srcAssets -o ./minAssets",
    "build": "node ./index.js"
  },
  "description": "Minifies and converts multiple SVGs into a single sprite sheet"
}
